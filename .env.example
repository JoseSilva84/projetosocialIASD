# Backend
DATABASE_URL=mongodb://localhost:27017/projetosocial
JWT_SECRET=your-strong-jwt-secret
CORS_ORIGIN=http://localhost:5173

# Grupos (opcional; se não definido, o seed de grupo não será criado automaticamente)
DEFAULT_GROUP_NAME=Boipeba
DEFAULT_GROUP_PASSWORD=strong-password-that-is-not-boi123
DEFAULT_GROUP_CREATED_BY=admin

# Admin inicial (opcional)
DEFAULT_ADMIN_NAME=admin
DEFAULT_ADMIN_PASSWORD=strong-admin-password

# Script de atribuição de participantes sem grupo
ASSIGN_GROUP_NAME=Boipeba
